# Implementation Plan: Change FastAPI Authentication to OAuth2PasswordBearer

## Phase 1: Core FastAPI Server Authentication Update
- [x] Task: Update Dependencies and Configuration (Red Phase) (76ff1d0)
    - [x] Write failing tests in `test/test_auth.py` that expect a Native JWT signed by the FastAPI server, and verify removal of OIDC specific config. (76ff1d0)
    - [x] Write failing test for the new `/token` endpoint expecting `username` and `password`. (76ff1d0)
- [x] Task: Implement Native JWT and `/token` endpoint (Green Phase) (76ff1d0)
    - [x] Update `src/common/configs/settings.py` to remove Authentik OIDC specific variables and add variables for PyJWT (secret key, algorithm). (76ff1d0)
    - [x] Create the `/token` endpoint in `src/core/server.py` using `OAuth2PasswordRequestForm`. (76ff1d0)
    - [x] Implement logic to verify credentials against Authentik IDP (e.g., via a back-channel request to Authentik). (76ff1d0)
    - [x] Implement logic to issue a Native JWT using PyJWT upon successful verification. (76ff1d0)
    - [x] Ensure tests pass. (76ff1d0)
- [x] Task: Update Security Dependencies (Green Phase) (76ff1d0)
    - [x] Replace existing OIDC dependency with `OAuth2PasswordBearer(tokenUrl="token")` in `src/common/middleware/auth.py`. (76ff1d0)
    - [x] Update JWT decoding logic to verify the Native JWT instead of the Authentik JWKS. (76ff1d0)
    - [x] Ensure all protected endpoints correctly authorize using the new token. (76ff1d0)
    - [x] Ensure tests pass. (76ff1d0)
- [x] Task: Remove Old OIDC Code (Refactoring) (76ff1d0)
    - [x] Delete any remaining OIDC login/callback endpoints and related session state logic from the backend. (76ff1d0)
- [x] Task: Conductor - User Manual Verification 'Phase 1: Core FastAPI Server Authentication Update' (Protocol in workflow.md) (76ff1d0)

## Phase 2: Template Updates
- [x] Task: Apply backend changes to templates (Red Phase) (76ff1d0)
    - [x] Write failing tests (or define manual checks) for the template generation to ensure templates lack the old OIDC code. (76ff1d0)
- [x] Task: Update template files (Green Phase) (76ff1d0)
    - [x] Replicate the changes made in Phase 1 to all project templates in `templates/*/`. (76ff1d0)
    - [x] Ensure the generated code uses `OAuth2PasswordBearer` and Native JWTs. (76ff1d0)
    - [x] Ensure tests pass for the generated projects. (76ff1d0)
- [x] Task: Conductor - User Manual Verification 'Phase 2: Template Updates' (Protocol in workflow.md) (76ff1d0)

## Phase 3: Streamlit UI Updates
- [x] Task: Update Streamlit Authentication UI (Red Phase) (76ff1d0)
    - [x] Define tests or a clear manual verification process for the new Streamlit login flow. (76ff1d0)
- [x] Task: Implement Standard Login Form (Green Phase) (76ff1d0)
    - [x] Remove the "Login with Authentik" redirect logic in `src/utils/auth_streamlit.py`. (76ff1d0)
    - [x] Create a Streamlit form with `st.text_input` for Username and Password. (76ff1d0)
    - [x] Implement the submit action to make a POST request to the backend `/token` endpoint. (76ff1d0)
    - [x] Store the received Native JWT in Streamlit's session state securely. (76ff1d0)
    - [x] Ensure the app correctly identifies the authenticated user from the Native JWT. (76ff1d0)
- [x] Task: Conductor - User Manual Verification 'Phase 3: Streamlit UI Updates' (Protocol in workflow.md) (76ff1d0)